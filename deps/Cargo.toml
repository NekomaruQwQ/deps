[package]
name = "deps"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
repository = "https://github.com/Nekomaru-PKU/deps"
description = "Curated and battle-tested dependencies and preludes that every project may need."

[dependencies]
env = { version = "1.0", optional = true }

[features]
default = []
dep-env = ["dep:env"]
